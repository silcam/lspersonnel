<div class="page-header">
  <h3><%= t(:New_research_permit_for) %> <%= @person.first_name %> <%= @person.last_name %></h3>
</div>

<%= form_for @research_permit, url: person_research_permits_path(@person) do |f| %>
<%= render "form", form: f %>
<% end %>
